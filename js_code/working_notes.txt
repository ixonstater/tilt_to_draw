Screens:
  1) Welcome screen
  2) Drawing creation widget
  3) Options screen
    1) Color picker widget
  4) About screen

Drawing Screen API:
  -Necessary Functions
    1) createCanvas()
    2) saveImg()
    3) drawToCanvas()

ToDo:
	2) Find way around while(x == null){ getx(); } loop
	3) Merge path to bitmap on bg thread every 10000(?) points added
  4) Add touch recognition and requisite methods to allow moving the circle w/o drawing to screen
  5) Create simple tutorial 
    -After 5 seconds of no recognized touch fade in the "touch and hold to draw" message
    -Persist message for 2 seconds then disappear
  6) Create color selection screen
  6.5) Create circle size selection screen
    -three or four selectable sizes
  7) Create buttons for options screen
  8) Investigate react-native-orientation


  Issue number 3
    -Cannot easily convert android.graphics.Path to bitmap
    -Can place the bitmap in a canvas and then draw the path to the bitmap